[skipped_groups.current_sld_failures]
reason = "Known current sld failures retained as regression sentinels."
tests = [
  "common-symbols.sh", # Unreferenced ELF COMMON symbols are dropped instead of being emitted.
  "common-gc.sh",      # The non-GC control link already drops the unreachable COMMON symbol.
]
